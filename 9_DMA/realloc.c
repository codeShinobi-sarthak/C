// “realloc” or “re-allocation” method in C is used to 
// dynamically change the memory allocation 
// of a previously allocated memory.
// syntax - ptr = realloc(ptr, newSize);
// where ptr is reallocated with new size 'newSize'.