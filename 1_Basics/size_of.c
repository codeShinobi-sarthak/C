#include <stdio.h>

int main(){
    int intergerType;
    char charType;
    float floatType;
    double doubleType;

    printf("size of int is : %d\n", sizeof(intergerType));
    printf("size of int is : %d\n", sizeof(charType));
    printf("size of int is : %d\n", sizeof(floatType));
    printf("size of int is : %d\n", sizeof(doubleType));
}